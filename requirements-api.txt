# Lean dependencies for the FastAPI serving image (Phase 3).
#
# The deployed model is a SINGLE LightGBM pipeline (sklearn Pipeline:
# OrdinalEncoder -> LGBMRegressor), so the serving image needs neither
# xgboost nor catboost — and none of streamlit / shap / jupyter / matplotlib.
#
# Versions pinned to what api/ is actually developed and tested against, so
# the container behaves exactly like the dev environment.
fastapi==0.139.0
uvicorn[standard]==0.51.0
pydantic==2.12.5
scikit-learn==1.8.0
lightgbm==4.6.0
joblib==1.5.2
pandas==2.3.3
numpy==2.3.5
